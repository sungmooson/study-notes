# test_0328



## Overview
정보처리기사 실기 대비 및 향후 SQLD 학습을 위하 SQLite3 + DBeaver 환경에서 SQL 기초 문법을 실습했다.  
단순 문법 암기보다 관계형 데이터베이스 개념과 관계대수 관점까지 연결해서 학습하는 데 집중했다.

# Retrospective
이번 실습을 통해 SQL의 기본흐름인 
SELECT -> WHERE -> GROUP BY -> HAVING -> ORDER BY
구조를 익혔고, JOIN과 집계 함수 사용에 대한 기초 감각을 잡을 수 있었다.